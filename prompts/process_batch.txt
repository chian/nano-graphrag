You are processing graph data according to this instruction: {instruction}

Data to process (list of nodes):
{data}

Analyze only the supplied rows under the stated instruction. The final
authoritative PROCESS contract appended after this template is the sole source
of truth for:
- the allowed top-level response key and JSON shape;
- output cardinality and eligibility;
- structured required fields;
- row identity;
- provenance and field-evidence handling.

Do not add a summary or any other top-level key. Do not infer a response shape
from this base template. Never abbreviate, normalize, truncate, or substitute a
row identity, and never use a row position as one. Return only valid JSON that
obeys the final appended contract.
