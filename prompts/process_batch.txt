You are processing graph data according to this instruction: {instruction}

Data to process (list of nodes):
{data}

Instructions:
1. Analyze the supplied rows' content (name, description, properties) according to the instruction
2. If the instruction asks you to normalize, extract, create, emit, produce, or materialize table rows with exact columns:
   - Return rows shaped like the requested table rows, not category labels.
   - Populate the exact requested column names at the top level of each processed item.
   - Return only `processed_items`; do not return `included`/`excluded` filter decisions.
   - When the instruction asks for one output per input row, return exactly one `processed_items` entry for every `Row N` and use each `row_N` or displayed row ID exactly once.
   - Set each output `id` from the row header only, such as `Row N (ID: <this value> -> normalized: ...)`; do not use IDs from nested fields inside that row.
   - When the row instruction includes inclusion/exclusion criteria, omit ineligible rows and emit only the materialized eligible rows in `processed_items` unless the instruction explicitly asks for one output row for every input row.
   - You may emit synthetic stable row IDs such as "row_1" when the output row combines evidence from multiple inputs.
   - Preserve source_refs, source_chunks, evidence_gap, deduplication_key, and every other requested table column.
   - Emit fewer, equal, or more processed_items than input rows as needed for the requested table grain.
   - If you include table_name, it must be the target table named by the instruction; do not emit rows for any sibling table.
3. Otherwise, choose an INTUITIVE field name based on what you're doing:
   - For vowel detection: use "starts_with_vowel" (boolean)
   - For first name extraction: use "first_name" (string)
   - For author extraction: use "author_name" (string)
   - For filtering: use "matches_criteria" (boolean)
   - For counting: use "count_value" (number)
   - For classification: use "category" (string)
3. Return a JSON object with this EXACT structure:
{{{{
  "processed_items": [
    {{{{"id": "node_a1b2c3d4", "name": "John Smith", "FIELD_NAME": value, "reason": "explanation"}}}},
    ...
  ],
  "summary": {{{{
    "total_processed": 0,
    "processing_type": "description of what was processed",
    "field_created": "intuitive_field_name"
  }}}}
}}}}

CRITICAL REQUIREMENTS:
- Replace "FIELD_NAME" with an intuitive field name (see examples above)
- For table normalization, replace the example fields with the exact requested table columns
- Field values must match the expected type (boolean, string, number)
- Use the normalized ID (the part after "normalized:") from the node description, NOT the original ID with quotes, unless creating a synthetic normalized table row
- Return ONLY valid JSON, no extra text or comments
- If a node cannot be processed, set the field to null and explain why
- The field name should clearly indicate what the value represents

FIELD NAMING EXAMPLES:
- "extract first names" → use "first_name" field
- "filter where first name starts with vowel" → use "starts_with_vowel" field  
- "count occurrences" → use "count_value" field
- "classify as author or not" → use "is_author" field

Be thorough in your analysis and provide clear reasoning for each decision.
